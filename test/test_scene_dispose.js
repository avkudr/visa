const chai = require('chai');
const assert = chai.assert;
const Scene3D = require('../src/scene3d.js').Scene3D;

describe('Dispose scene', () => {
    it('deleting camera frustrums', () => {
        assert.equal(1, 0);
    }); 
});
const chai = require('chai');
const assert = chai.assert;

describe('Main', () => {
    it('should start empty', () => {
        // Test implementation goes here
        assert.equal(1, 0);
    }); 
});